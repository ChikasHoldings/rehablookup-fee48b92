/**
 * RouteChangeTracker — no-op stub.
 *
 * Google Analytics has been removed from this platform.
 * This component is retained as a mount point so existing imports compile.
 * To integrate a new analytics provider, implement page-view tracking here.
 */
export function RouteChangeTracker() {
  return null;
}
