/**
 * Session Management Tab for Provider Settings
 * Now uses the shared SessionManagementCard component
 */
import { SessionManagementCard } from "@/components/shared/SessionManagementCard";

export function SessionManagementTab() {
  return <SessionManagementCard />;
}
